Classes and IDs in HTML

Can target specific target code to do specialized
modifications

an id is unique, ONLY ONE PER HTML ELEMENT / one id per age

classes can be used multiple times

a tag:
defines a hyperlink, can used to link another page or an another section in the same page

    href=""
    attribute where indicates the link location. can be used with id=""